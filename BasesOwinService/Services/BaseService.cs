namespace BaseOwinService.Services
{
    using System.Web.Http.Dependencies;

    public class BaseService
    {
        // TODO: implement IDbContext
        //private IDbContext _context;

        //public BaseService(IDependencyResolver dependencyResolver)
        //{
        //    DependencyResolver = dependencyResolver;
        //}

        //protected IDependencyResolver DependencyResolver { get; }

        //protected IDbContext Context
        //    => _context ?? (_context = (IDbContext) DependencyResolver.GetService(typeof (IDbContext)));
    }
}